<%= form_for @comment do |f| %>

<div class="form-group">
	<label>Balance ton commentaire</label>
	<%= f.text_field :content, class: 'form-control' %>
</div>
<div class="form-group">
	<button type="submit" class="btn btn-dark">Modifier le gossip</button>
</div>

<% end %>

<h1>Bonjour les #GossipAddicts,</h1>

<p>***Tu peux poster un nouveau gossip dans le formulaire.***</p>

<% if @gossip.errors.any? %><div class="alert alert-danger" role="alert">
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
	</ul></div>
<% end %>



<html>
  <body>
    <!-- ... -->

    <%= form_tag url_for(action: 'create'), method: "post" do %>

      <%= label_tag 'Titre du gossip' %><br>
      <%= text_field_tag 'title', @title %>
      <br /> <br />

      <%= label_tag 'Dis nous tout' %><br>
      <%= text_area_tag 'content', @content %>
      <br /> <br />

      <%= submit_tag "Envoi le gossip" %>

    <% end %>

    <!-- ... -->
  </body>
</html>